«адание:

Ќаписать сервис на python2.7
—ервис имеет один endpoint:

```
GET /from_cache?key="some key"
```

который возвращает значение получаемое от стороннего сервиса :

https://vast-eyrie-4711.herokuapp.com/?key=123

«начение нужно закешировать и вернуть пользователю. ѕри этом клиент ждет ответа не более 1 сек (после этого он отвалитс€ по таймауту), но может перезапрашивать значение несколько раз. “ак как сервис довольно медленный, и может отвечать с ошибками (error - это ошибка которую не нужно кешировать), желательно свести количество обращений к нему к минимуму. «начение из кеша должно быть не старше 24 часов. —ервис не должен п€тисотить или падать. “есты необ€зательны. ≈сли требуютс€ какие-то дополнительные действи€ дл€ запуска - описать их пошагово в readme.